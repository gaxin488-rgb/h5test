H5 TASK REPORT
===============================
Title: Server SSH/network, client-IP mapping, zone checks, Python3, SQLite and Erlang/BEAM
Date: 2026-09-16
Scope: D:\h5\codex_workspace and the configured H5 server

SECURITY NOTE
-------------
This report is intended for the public GitHub repository. Passwords, private keys,
tokens and exact internal addresses are intentionally omitted or redacted.

1. SSH ACCESS
-------------
- Installed the local SSH public key on the server authorized_keys file.
- Updated the local SSH configuration to prefer public-key authentication and
  verified non-interactive access with BatchMode.
- Confirmed the remote account and hostname through SSH.
- No private key, password or token was copied into the repository.

2. NETWORK RECOVERY
-------------------
- Restored the server default route and DNS configuration.
- Persisted the network settings in the active interface configuration.
- Verified SSH connectivity and outbound repository/download access after the
  network correction.
- Exact internal IP, gateway and DNS values are omitted from this public report.

3. CLIENT / ADB / IP MAPPING
----------------------------
- Checked the ADB client list and found emulator-5556 online; emulator-5554 was
  not online during the check.
- Checked the emulator address and installed H5 client package.
- The current emulator was not running the H5 client in the foreground, so no
  live request was attributed to that emulator at the time of the final check.
- Server logs showed the client calling the role API with the aliases local_1 and
  symlf_1. Both resolved to the same symlf_1 server mapping and returned HTTP 200;
  no client/server IP mismatch was proven by the captured requests.

4. ZONE STATUS AND MAPPING
--------------------------
- Checked the configured zones:
  sszg_center_0, sszg_symlf_1, sszg_symlf_2, sszg_symlf_3 and sszg_symlf_4.
- Verified each ctl.sh runtime selector and its Erlang distribution/game port:
  center 9000, symlf_1 9001, symlf_2 9002, symlf_3 9003 and symlf_4 9004.
- Verified the web listener layout on the server for the expected zone ports.
- ctl.sh does not implement a status command; the prior status probe returned
  the script's unknown-command response.
- At the final pre-Erlang check there were no Erlang zone processes and no screen
  sessions. Zones were not auto-started as part of the runtime installation.

5. HTTP 403 / API CHECK
-----------------------
- Reviewed the API request/log path related to the reported 403 issue.
- The representative role API requests captured during verification returned
  HTTP 200, including the local_1 and symlf_1 aliases.
- No new HTTP 403 was present in the final evidence; a broad client-side scan was
  not claimed because the current emulator was not running the H5 client.

6. PACKAGE AND RUNTIME REPAIR
-----------------------------
- Repaired CentOS 7.9 Vault repository configuration and disabled stale repo
  definitions that prevented package resolution.
- Completed the planned bulk package upgrade: 167 package upgrades plus the
  required package/dependency additions reported by the dry run.
- The newer kernel was installed but the server was not rebooted.
- Added Python 3 from the configured SCL runtime. Final verified runtime was
  Python 3.6.12 with the existing pip command available.
- Installed SQLite runtime and development packages and verified libsqlite3 is
  visible to the system linker.

7. ERLANG / BEAM
----------------
- Built official Erlang/OTP 19.2 source on the CentOS 7 server with the existing
  GCC/OpenSSL/ncurses build prerequisites.
- Installed it at:
  /opt/erlang/otp19.2
- Verified ERTS/BEAM version 8.2 and OTP release 19.
- Added global commands through /usr/local/bin:
  erl, erlc, epmd, beam and beam.smp.
- Verified that all five zone ctl.sh scripts now select:
  /opt/erlang/otp19.2/lib/erlang/bin/erl
- Source archives, build logs and local temporary download files were removed
  after successful verification.
- Official release reference: https://www.erlang.org/patches/otp-19.2

8. FINAL STATE / LIMITS
-----------------------
- SSH/network, package, SQLite, Python3 and Erlang/BEAM checks completed.
- The runtime was installed without changing zone configuration or starting the
  zones.
- A server reboot was intentionally not performed.
- Added a local Agent.md rule requiring a redacted task report, atomic GitHub
  commit verification and cleanup of the local temporary report afterward.
- No credentials or private authentication material are included in this report.

