--------------------------------------------
-- 
-- 
-- @Date    : 2021.05
-- @description    : 
		-- 勇者之路 编队 item
--------------------------------- 
local _controller = BraveRoadController:getInstance()
local _model = _controller:getModel()
local _table_insert = table.insert

BattleFormListItem = class("BattleFormListItem", function()
    return ccui.Widget:create()
end)

function BattleFormListItem:ctor()
	self:configUI()
	self:register_event()
end

function BattleFormListItem:configUI( )
	self.size = cc.size(120, 180)
	self:setTouchEnabled(false)
	self:setAnchorPoint(cc.p(0.5, 0.5))
    self:setContentSize(self.size)

	local csbPath = PathTool.getTargetCSB("braveroad/battle_form_list_item")
    self.root_wnd = cc.CSLoader:createNode(csbPath)
    self:addChild(self.root_wnd)

    local container = self.root_wnd:getChildByName("container")
    self.container = container
end

function BattleFormListItem:register_event(  )
	registerButtonEventListener(self.container, function (  )
	end, true, 1)
end

function BattleFormListItem:setData( customs_vo, cfg_data, level_id)
	self:updateCustomsItem()
end

function BattleFormListItem:updateCustomsItem(  )
	if not self.cfg_data then return end
	
	-- 关卡图标
	-- 关卡章节
	-- 星数
	self:updateStarInfo()
end

function BattleFormListItem:updateStarInfo( )
end

function BattleFormListItem:DeleteMe(  )
	self:removeAllChildren()
    self:removeFromParent()
end