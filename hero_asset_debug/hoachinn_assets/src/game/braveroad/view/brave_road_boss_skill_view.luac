--------------------------------------------
-- 
-- 
-- @Date    : 2021.05
-- @description    : 
		-- 勇者之路副本关卡 技能预览 （弃用）
--------------------------------- 
local _controller = BraveRoadController:getInstance()
local _model = _controller:getModel()
local _table_insert = table.insert

BraveRoadBossSkillView = class("BraveRoadBossSkillView", function()
    return ccui.Widget:create()
end)

function BraveRoadBossSkillView:ctor()
	self:configUI()
	self:register_event()
end

function BraveRoadBossSkillView:configUI( )
	self.size = cc.size(120, 180)
	self:setTouchEnabled(false)
	self:setAnchorPoint(cc.p(0.5, 0.5))
    self:setContentSize(self.size)

	local csbPath = PathTool.getTargetCSB("braveroad/brave_road_boss_skill_view")
    self.root_wnd = cc.CSLoader:createNode(csbPath)
    self:addChild(self.root_wnd)

    local container = self.root_wnd:getChildByName("container")
    self.container = container
end

function BraveRoadBossSkillView:register_event(  )
	registerButtonEventListener(self.container, function (  )
	end, true, 1)
end

function BraveRoadBossSkillView:setData( customs_vo, cfg_data, level_id)
	self:updateCustomsItem()
end

function BraveRoadBossSkillView:updateCustomsItem(  )
	if not self.cfg_data then return end
	
	-- 关卡图标
	-- 关卡章节
	-- 星数
	self:updateStarInfo()
end

function BraveRoadBossSkillView:updateStarInfo( )
end

function BraveRoadBossSkillView:DeleteMe(  )
	self:removeAllChildren()
    self:removeFromParent()
end