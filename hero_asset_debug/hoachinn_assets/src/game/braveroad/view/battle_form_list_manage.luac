--------------------------------------------
-- 
-- 
-- @Date    : 2021.05
-- @description    : 
		-- 勇者之路 编队
--------------------------------- 
local _controller = BraveRoadController:getInstance()
local _model = _controller:getModel()
local _table_insert = table.insert

BattleFormListManage = class("BattleFormListManage", function()
    return ccui.Widget:create()
end)

function BattleFormListManage:ctor()
	self:configUI()
	self:register_event()
end

function BattleFormListManage:configUI( )
	self.size = cc.size(120, 180)
	self:setTouchEnabled(false)
	self:setAnchorPoint(cc.p(0.5, 0.5))
    self:setContentSize(self.size)

	local csbPath = PathTool.getTargetCSB("braveroad/battle_form_list_manage")
    self.root_wnd = cc.CSLoader:createNode(csbPath)
    self:addChild(self.root_wnd)

    local container = self.root_wnd:getChildByName("container")
    self.container = container
end

function BattleFormListManage:register_event(  )
	registerButtonEventListener(self.container, function (  )
	end, true, 1)
end

function BattleFormListManage:setData( customs_vo, cfg_data, level_id)
	self:updateCustomsItem()
end

function BattleFormListManage:updateCustomsItem(  )
	if not self.cfg_data then return end
	
	-- 关卡图标
	-- 关卡章节
	-- 星数
	self:updateStarInfo()
end

function BattleFormListManage:updateStarInfo( )
end

function BattleFormListManage:DeleteMe(  )
	self:removeAllChildren()
    self:removeFromParent()
end