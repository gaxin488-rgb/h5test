ArenaWorldEvent = ArenaWorldEvent or {}

-- 更新个人数据
ArenaWorldEvent.UpdateArenaMyBaseInfo = "ArenaWorldEvent.UpdateArenaMyBaseInfo"

-- 更新全部挑战对手数据
ArenaWorldEvent.UpdateAllArenaEnemyList = "ArenaWorldEvent.UpdateAllArenaEnemyList"

-- 对手信息
ArenaWorldEvent.GetArenaEnemyData = "ArenaWorldEvent.GetArenaEnemyData"

--布阵信息
ArenaWorldEvent.GetArenaTeamData = "ArenaWorldEvent.GetArenaTeamData"

-- 前三名数据
ArenaWorldEvent.UpdateArenaTopSixRoleData = "ArenaWorldEvent.UpdateArenaTopSixRoleData"

-- 排行榜数据
ArenaWorldEvent.UpdateArenaRankData = "ArenaWorldEvent.UpdateArenaRankData"

-- 我的记录
ArenaWorldEvent.UpdateArenaMyLogData = "ArenaWorldEvent.UpdateArenaMyLogData"

-- 大神风采
ArenaWorldEvent.UpdateArenaGodLogData = "ArenaWorldEvent.UpdateArenaGodLogData"

-- 天梯状态改变
ArenaWorldEvent.UpdateArenaOpenStatus = "ArenaWorldEvent.UpdateArenaOpenStatus"

-- 天梯红点改变
ArenaWorldEvent.UpdateArenaRedStatus = "ArenaWorldEvent.UpdateArenaRedStatus"

-- 录像播放
ArenaWorldEvent.GetArenaRecordInfo = "ArenaWorldEvent.GetArenaRecordInfo"

--布阵成功
ArenaWorldEvent.ARENAWORLD_SAVE_FROM_EVENT = "ArenaWorldEvent.ARENAWORLD_SAVE_FROM_EVENT"
