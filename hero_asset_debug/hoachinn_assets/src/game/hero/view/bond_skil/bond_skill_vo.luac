-- --------------------------------------------------------------------
-- 羁绊技的对象数据
-- 
-- (必填, 创建模块的人员)
-- (必填, 后续维护以及修改的人员)
-- @description:
--      羁绊技的对象数据     
-- <br/>Create: 2018年11月14日
-- --------------------------------------------------------------------
BondSkillVo = BondSkillVo or BaseClass(EventDispatcher)

function BondSkillVo:__init()
    self.id                 = 0   --  id
    self.lev                = 0   --  等级
    self.partner_id         = 0   --  装备英雄唯一id

end

function BondSkillVo:setUpData(data)
    -- self.lev = data.lv or data.lev
    -- self.star = data.star
    -- self.is_wakeup = data["break"] or data.is_wakeup
    -- self.is_speed = data.close_speed or data.is_speed
    -- self:setActivatedData(data)
end

