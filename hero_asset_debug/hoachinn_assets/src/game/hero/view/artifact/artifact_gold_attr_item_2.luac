--------------------------------------------
-- 
-- 
-- @Date    : 2019-06-24 15:18:54
-- @description    : 
		-- 金色秘卷属性词条
---------------------------------
ArtifactGoldAttrItem2 = class("ArtifactGoldAttrItem2", function()
    return ccui.Widget:create()
end)

ArtifactGoldAttrItem2.Width = 282
ArtifactGoldAttrItem2.Height = 37
function ArtifactGoldAttrItem2:ctor()
	self:configUI()
	self:registerEvent()
end

function ArtifactGoldAttrItem2:configUI(  )
    self.root_wnd = createCSBNote(PathTool.getTargetCSB("hero/artifact_gold_attr_item_2"))
	if not self.root_wnd then return end
    self.size = self.root_wnd:getContentSize()
    self:setAnchorPoint(cc.p(0, 0))
	self:setContentSize(self.size)
	self:setCascadeOpacityEnabled(true)

	self.root_wnd:setAnchorPoint(0.5, 0.5)
	self.root_wnd:setPosition(self.size.width/2, self.size.height/2)
	self:addChild(self.root_wnd)
	self._ui = getAllCsbExportChildren(self.root_wnd)
end

function ArtifactGoldAttrItem2:registerEvent(  )
end

function ArtifactGoldAttrItem2:setData(data)
    local attr_name = data[1] or data.attr_id
    local attr_val = data[2] or data.attr_val/1000
    if type(attr_name) == "number" then
        attr_name = Config.AttrData.data_id_to_key[attr_name]
        if not attr_name then
            attr_name = Config.AttrExtraData.data_id_to_key[attr_name]
        end
    end
    local icon = PathTool.getAttrIconByStr(attr_name)
    local res = PathTool.getResFrame("common", icon)
    loadSpriteTexture(self._ui._attr_icon, res, LOADTEXT_TYPE_PLIST)
    local _txt = Config.AttrData.data_key_to_name[attr_name]
    if not _txt then
        _txt = Config.AttrExtraData.data_key_to_name[attr_name]
    end
    local num = 10
    if judgingLanguage() then
        num = 5
    end
    self._ui._attr_name:setString(transformTextToShort(_txt,num))
    addEvt2showAllTextTips(self._ui._attr_name,_txt,num,nil,nil,nil,nil,true)
    local is_per = PartnerCalculate.isShowPerByStr(attr_name)
    if is_per == true then
        attr_val = (attr_val/10).."%"
    end
    self._ui._attr_value:setString(attr_val)
end

function ArtifactGoldAttrItem2:DeleteMe(  )

	self:removeAllChildren()
	self:removeFromParent()
end