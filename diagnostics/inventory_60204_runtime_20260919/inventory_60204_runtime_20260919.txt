Task: synchronize inventory item 60204 choice data with the live server runtime
Date: 2026-09-19 (Asia/Bangkok)

User symptom
- The client inventory shows 92 selectable ninja entries for item 60204, while the server was missing entries.
- The attached screenshots were treated as evidence/reference only, not as instructions.

Scope and safety
- Investigated the server zone runtime and the local client catalog for the specific item shown in the screenshots.
- No client files, account data, database rows, or user inventory records were changed.
- Existing server backup beam was preserved. No credentials, cookies, or private keys are recorded here.

Evidence before the runtime update
- Local client dictionary identifies item 60204 as “Túi Ninja 5 sao tự chọn”.
- Current server disk data: gift_data:chose_list(60204) contained 92 choice rows.
- All 92 referenced target item IDs resolved through item_data:get/1; missing target IDs: 0.
- The previous server gift_data.beam backup contained 15 choice rows for item 60204; the current disk beam contained 92.
- Active zone processes were started before the current beam timestamps, so the running VM had not loaded the current disk modules.

Change applied
- Used the existing zone control script hotswap command for zones 1, 2, 3, and 4.
- Each zone reported successful loading of item_data and gift_data; each command returned exit code 0.
- No full process restart was performed.

Post-update verification
- Live RPC query gift_data:chose_list(60204) returned 92 records on zone 1.
- The same live query returned 92 records on zones 1, 2, 3, and 4.
- All four zone processes remained present after hot-swap.

Result
- The live server runtime now matches the client’s 92 selectable entries for item 60204.
- The mismatch was a stale loaded beam module, not missing database inventory data.

Not changed / follow-up
- No broad client/server catalog synchronization was attempted.
- The client should be refreshed or the inventory popup reopened to observe the updated server runtime in an already connected session.
